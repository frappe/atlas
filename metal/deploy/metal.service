[Unit]
Description=metal daemon
After=network.target

[Service]
# Runs as root: needs the systemd D-Bus, network namespaces and jailer.
ExecStart=/usr/bin/metald
Restart=on-failure
RestartSec=1

[Install]
WantedBy=multi-user.target
