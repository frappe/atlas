name: Atlas Metal tests

on:
  pull_request:
    paths:
      - "metal/**"
      - ".github/workflows/metal-tests.yml"
  push:
    branches:
      - develop
    paths:
      - "metal/**"
      - ".github/workflows/metal-tests.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: metal
    steps:
      - name: Check out source
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Set up Go
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5
        with:
          go-version-file: metal/go.mod
          cache-dependency-path: metal/go.sum

      # The binary embeds the API specification, so generate it before any build.
      - name: Generate the API specification
        run: make openapi

      - name: Check formatting
        run: test -z "$(gofmt -l ./cmd ./internal)" || { gofmt -l ./cmd ./internal; exit 1; }

      - name: Vet
        run: go vet ./...

      - name: Run unit tests with the race detector
        run: go test -race ./...

      - name: Build the release binaries
        run: make build
