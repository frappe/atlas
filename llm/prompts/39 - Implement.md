mplement the plan. Implement one phase at a time.

The plan is carefully written, but you might face issues during implementation. Follow the intent, adapt to what you find. Think deeply and promptly present the issues.

Use frappe-dev skills for all framework-related changes.

For each step, create a to-do list to track your progress. Pick up the unchecked items from the to-do list. Check the todos and update the plan as you progress.

After each step, generate the wireframe based on the implementation. Track planned vs actual drift in a file.

Verify the code with unit tests after each phase. Fix issues before proceeding. 

If we're making major changes to the flow, then update or add e2e tests.  

Run e2e tests in the end.

Update the spec.