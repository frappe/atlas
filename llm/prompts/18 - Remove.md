The plan documents were created to implement the Atlas app.

Now that we've already done with the implementation. I want to get rid of plan files. The spec should represent everything about the repository. Spec should be the source of truth.

If the plans can't be folded into the spec, then write a small file noting the issues. Refer to the @plan/spec-fold-notes.md for folding issues.

Read the base plan @plan/00-overview.md  . Move the details to spec and delete the plan files.

There are files in plan directory not mentioned in the overview. Ignore them. We'll handle them separately.
